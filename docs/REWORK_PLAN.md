# eht_mk2 설계 기록

BTC/ETH USDⓈ-M 선물 자동매매. 이 문서는 **왜 지금 구조가 됐는지**와 **검증에서 배운 것**만 남긴다.
파일 구조와 실행법은 `README.md`.

## 0. 현재 상태

- 라이브: BTC/USDT **1d 국면전환** (BULL 듀얼모멘텀 · BEAR EMA크로스 12/50 · SIDEWAYS Z-Score), 데모 계좌
- 주문: 전량 시장가. SL/TP는 봇 내부 폴링으로 관리 (§5 참조)
- 권고: **A(현행) + B(ETH 1d 변동성타겟 추세) 동일 비중** — 배포 전 작업은 §6

## 1. 출발점 (2026-09-17 진단)

| 문제 | 영향 |
|---|---|
| income 조회에 기간 인자 없음 → 최근 7일만 집계 | 주간 리포트의 누적 수익률이 전부 7일 롤링 값 |
| 티커가 None이면 `float(None)` 예외 | 약 한 달간 신규 진입 불가 (로그 65,900회) |
| 봇 정지 감지 없음 | 47일 방치된 포지션의 우연한 수익이 성과로 집계 |
| 지정가→시장가 폴백 경로에서 기록 누락 | `trade_logs` 구멍 |
| 백테스트와 라이브의 국면 판정·전략 구성이 서로 다름 | 백테스트 수치를 믿을 수 없음 |

## 2. 고친 것

| 영역 | 조치 |
|---|---|
| 손익 집계 | 7일 창 페이징, ROI 기준을 마지막 충전 시점으로 (`tools/calculate_total_pnl.py`) |
| 주문 | 지정가 추격 제거, 전량 시장가. 티커 실패 시 사이클만 건너뜀 |
| 감시 | 하트비트 파일 + `tools/check_heartbeat.py` (cron용) |
| 기록 대조 | `tools/reconcile_trades.py` (거래소 체결 ↔ DB) |
| 경로 통일 | 국면 확정을 `confirm_regimes()` 하나로. 라이브·백테스트·전략 클래스에 있던 세 벌의 사본(기본값·초기상태 제각각) 제거 |
| 설정 반영 | `RegimeSwitchingStrategy`가 설정의 전략 이름·파라미터를 무시하던 버그 수정 — "라이브 설정 백테스트"가 실은 기본값으로 돌고 있었다 |
| 폴더 | `core/ live/ research/ tools/ config/ data/ logs/`, 경로는 `core/paths.py` |

## 3. 백테스트 엔진 (`research/backtester.py`, `research/binance_env.py`)

| 항목 | 방식 |
|---|---|
| 봉 안 순서 | 시가 체결 → 봉 중 청산/SL/TP → 펀딩 (구버전은 역순이었다) |
| 수수료 | taker. 보수 프로필 0.04%(데모 청구 요율), 실계좌는 **0.05%** |
| 슬리피지 | 보수 프로필 `1bp + 0.02×ATR%`, 현실 프로필 `1bp + 0.005×ATR%` (§4 비용 기준) |
| 펀딩 | 8시간 실제 이력 (4년치 캐시) |
| 청산 | 바이낸스 MMR 브래킷 공식 |
| 정밀도 | 틱 0.1 / 스텝 0.001 / 최소명목가 50 USDT |
| 선택 기능 | 변동성 타겟팅(`target_vol`), ATR 손절(`atr_sl_mult`) |
| 멀티 종목 | `research/portfolio_backtester.py` (BTC/ETH 로테이션용) |

엔진 검산: `tools/selftest_engine.py` 19건 (손익·최소명목가·진입봉 SL·SL 우선·슬리피지 방향·펀딩 부호·청산가).

## 4. 검증 방법과 교훈

**방법** — 인샘플 6개월 → 아웃샘플 2개월 롤링 워크포워드. 아웃샘플만 집계한다.
파라미터는 상위 30% 조합의 중앙값(고원)으로 고른다. 전략 선택까지 탐색할 때는
중첩 워크포워드(폴드마다 인샘플만 보고 선택)를 쓴다.

**교훈** — 좋아 보였던 결과는 대부분 오류였다. 크기를 남긴다.

| 착오 | 겉보기 | 교정 후 |
|---|---|---|
| 국면별 전략을 아웃샘플 성적으로 선택 (선택 편향) | 1d +110% | **-47%** |
| 로테이션 엔진이 i봉 종가 신호를 i봉 시가에 체결 (룩어헤드) | 샤프 4.26 | **-0.35** |
| 캐리 리밸런싱이 현금을 음수로 둠 (무이자 차입) | 연 11% | **연 5%** |
| 탐색 축을 9개 → 162개로 확대 (과적합) | — | 아웃샘플 +76% → **+57%** |
| B&H를 폴드별 복리로 이어붙임 (구간 사이 공백 누락) | B&H +53% | **+73%** |
| 결합 비교의 '공통구간'이 결측을 0으로 채운 합집합이었음 | A+B+C+D 샤프 0.96 | **1.10** (같은 비용, 진짜 공통구간 2.62년) |
| 데이터를 8개월 늘림 | 샤프 0.98 | **0.83** — 이 규모의 샤프는 ±0.15씩 움직인다 |

**비용 기준** — 초기에는 "비용 2배에서도 수익" 을 합격선으로 썼다.
그런데 실측해 보니 BTCUSDT는 $50만 시장가도 충격 0.1bp라, 보수 프로필 슬리피지(1d 편도 7.8bp)가
이미 수십 배 비관적이었다. 비용 2배는 1d 기준 현실 비용의 약 4.7배다.
고회전 전략을 과하게 탈락시키는 기준이었으므로 **현실 프로필 + 1.5배 여유**로 바꿨다.
단 실계좌 수수료(0.05%)가 데모(0.04%)보다 높다는 점은 반대 방향으로 반영한다.

## 5. 운영상 알려진 문제

- **데모 계좌는 조건부 주문(STOP_MARKET/TAKE_PROFIT_MARKET)을 만들지 않는다.** ID까지 돌려주지만
  주문 이력에 남지 않는다. 봇이 이를 감지해 경고하고, SL/TP는 30초 폴링으로만 관리된다.
  → 봇이 멈추면 포지션이 무방비다. 하트비트 감시 필수. 실계좌에서는 거래소 스탑이 동작한다
- `RegimeLiveTrader`가 `super().__init__()`을 부르지 않는다. 새 상태는 `LiveTrader`의 클래스 속성으로 둘 것
- 데스크탑(WSL)은 ssh가 끊기면 배포판 전체가 내려간다 (tmux·nohup 모두 소용없음). 긴 작업은 연결을 유지한 채 돌린다

## 6. 전략 탐색 결과와 권고

**타임프레임** — 1h~3d 전수 탐색. 국면별 전략을 탐색해 조합하는 방식은 중첩 워크포워드에서
**전 타임프레임 손실**이었다. 남은 건 1d 국면전환과 변동성타겟 추세 계열뿐이다. 차이를 만든 건
회전율이다 — 4h 이하에서는 비용이 신호를 넘는다. 현실 비용으로 다시 봐도 4h 국면전환(E)은 샤프 0.39다.
즉 비용 기준을 완화해도 탈락했던 단기 전략이 살아나지는 않았다.

**계열별**

| 계열 | 결과 |
|---|---|
| 변동성 타겟팅 · ATR 손절 | 유효 |
| BTC/ETH 횡단면 로테이션 | 전 TF 샤프 음수 — 종목 교체 비용이 이득을 먹는다 |
| 펀딩 방향 베팅 · 급변 후 역추세 | 실패 |
| 델타중립 캐리 (현물 롱 + 선물 숏) | 연 5% (lev3), 무위험 대비 초과 1.3%. 샤프 12는 꼬리위험(거래소 파산·ADL)이 표본에 없어서 나온 착시. 레버리지 5배부터 청산 |

**결합** — 현실 비용, 공통구간 2023-12-11 ~ 2026-07-23 (2.62년)

- A 국면전환 BTC 1d (현행) · B 변동성타겟 추세 ETH 1d · C 변동성타겟 추세 BTC 4h · D 변동성타겟 추세 BTC 3d
- A–B 일간수익률 상관 0.29

| 구성 | 연환산 | 샤프 | 샤프(rf 4%) | MDD | 비용1.5배 샤프 |
|---|---|---|---|---|---|
| A 단독 | 27.8% | 1.07 | 0.91 | -27.3% | 0.82 |
| **A+B** | **28.0%** | **1.16** | **0.99** | **-19.8%** | 0.87 |
| A+B+C | 28.2% | 1.16 | 0.99 | -17.7% | 0.94 |
| A+B+C+D | 23.2% | 1.20 | 1.00 | -14.3% | 0.99 |
| B 단독 | 24.2% | 0.72 | 0.60 | -42.8% | 0.48 |

샤프 2 이상에 높은 수익을 내는 조합은 없었다. 샤프(초과수익 기준)는 레버리지로 바뀌지 않는다.

### 권고: A + B 동일 비중

- 수익은 A 단독과 같고(28.0% vs 27.8%) 낙폭이 -27% → -20%로 준다
- 종목이 달라 한 계정에서 두 포지션이 서로 간섭하지 않는다. 둘 다 일봉이라 회전율이 낮고 비용에 강하다
- 샤프 차이(1.07 → 1.16)는 추정 오차(±0.15) 안이다. **확실한 이득은 낙폭 감소**다
- B 단독은 낙폭 -43%로 쓸 만하지 않다. 결합해서만 가치가 있다
- C를 더하면(A+B+C) 낙폭이 조금 더 줄지만 C는 A와 같은 BTC 선물이라 포지션이 합쳐진다.
  손익은 선형이라 합산해도 결과가 같고(넷팅은 오히려 수수료를 줄인다) 백테스트는 보수적이지만,
  슬리브별 수량·손절을 따로 추적하는 실행기가 필요하다. 다음 단계로 둔다

### A+B 배포 전에 필요한 것

1. 라이브 트레이더에 변동성 타겟 사이징(`target_vol`)과 ATR 손절 구현 — 지금은 고정 배분·고정 % 손절뿐이다
2. 봇 인스턴스 2개 실행 — 락·하트비트·상태 파일이 전역 하나라 설정별로 분리해야 한다
3. 슬리브별 자본 비율(`capital_fraction: 0.5`) — 지금은 두 봇이 각자 전체 잔고 기준으로 사이징한다
4. B의 워크포워드 파라미터를 합의치로 고정하고 설정 파일 생성
5. 데모에서 신호가 백테스트와 일치하는지 확인

## 7. 보류

- 워뇨띠 거래내역(2018-03~2021-12, 비트멕스) 분석 — 나중에. 기계적 근사("급변 후 역추세")는
  2023~2026에서 BTC 샤프 0.05 / ETH -1.10으로 작동하지 않았다 (`research/wonyotti_fade.py`)
